#ifndef SCREEN_H
#define SCREEN_H

enum Screen
{
    SCREEN_WIDTH = 800,
    SCREEN_LENGTH = 800,

    // Position of next piece in screen 
    NEXT_PIECE_X = 9,
    NEXT_PIECE_Y = 3,

    // Position of stored piece in screen 
    STORED_PIECE_X = 9,
    STORED_PIECE_Y = 10
};

#endif 
